import React from 'react';


const header = (props) => (
  <div style={{fontSize: '36px',
               transform: 'rotate(180deg)',
               textAlign: 'center',
               width: '100vw',
               height: '100px',
               lineHeight: '100px'}}>
    Welcome back!
  </div>
)

export default header;
