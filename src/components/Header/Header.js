import React from 'react';


const header = (props) => (
  <div style={{fontSize: '36px',
               transform: 'rotate(180deg)',
               textAlign: 'center'}}>
    Welcome back!
  </div>
)

export default header;
